---
- name: Firewall - Allow Splunk Cluster Manager ports
  ansible.posix.firewalld:
    port: "{{ item }}"
    permanent: true
    state: enabled
    immediate: true
  loop: "{{ splunk_cluster_manager_ports }}"
  notify:
    - Reload firewalld