---
- name: Firewall - Allow Splunk Deployer ports
  ansible.posix.firewalld:
    port: "{{ item }}"
    permanent: true
    state: enabled
    immediate: true
  loop: "{{ splunk_deployer_ports }}"
  notify:
    - Reload firewalld