---
- name: Firewall - Allow Splunk Heavy Forwarder ports
  ansible.posix.firewalld:
    port: "{{ item }}"
    permanent: true
    state: enabled
    immediate: true
  loop: "{{ splunk_heavy_forwarder_ports }}"
  notify:
    - Reload firewalld