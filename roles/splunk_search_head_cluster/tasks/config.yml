---
- name: SHC - Deploy server.conf configuration
  ansible.builtin.template:
    src: server.conf.j2
    dest: "{{ splunk_home }}/etc/system/local/server.conf"
    owner: "{{ splunk_user }}"
    group: "{{ splunk_group }}"
    mode: "0644"
  notify:
    - Restart Splunk